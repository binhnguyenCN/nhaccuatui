import React from 'react';

import styles from './styles.module.scss';

const WeeklyArtist = () => (
  <article className={`${styles['weekly-artist-category']}`}>
    <h1>WEEKLY ARTIST CATEGORY</h1>
  </article>
);
export default WeeklyArtist;
